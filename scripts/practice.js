// 1) create an array with the student's names
let names=["name1","name2"];
// 2) display the names on the HTML in a ul
    // *** function, for, getElementId, concatenation +=

//3)create an array of objects (name,salary)
let users = [
    {
        name:"Hethe",
        salary:10000
    },
    {
        name:"Hethe",
        salary:10000
    },
];
//4)display in a table name, salary
function displayUsers(){
    for... 
    {
        row+=`<tr>
        <td>${}</td>
        </tr>`;
    }

    document. ... inneHMTL = row
}
//5) display the total (sum the salaries)